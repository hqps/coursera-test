Sublime


type HTML at top of new document, then CTRL + Space to get a basic html doc set 




CMD is same as TERMINAL on mac

dir = ls on mac



git status = to check what's going on in git local

cd .. = goes one directory up


GitHub
To get something to be added to your Git database (on local) you first need to make it to be committed.

Do this by  git add .     the (.) dot adds everything in the local folder